/**
 * Icon assets in public/icons/ are maintained manually — do not generate or overwrite them.
 * This script is a no-op so `npm run generate-icons` does not destroy your PNG/SVG/ICO files.
 */
console.log(
  'generate-icons: skipped — use the files in public/icons/ (see layout.tsx / manifest.json).'
)
process.exit(0)
