---
description: High-level deploy / branch conventions
globs: ["**/*"]
alwaysApply: true
---

# Deployment Protocol

- **`dev`:** Staging / testing environment (`dev.buddget.online`).
- **`main`:** Production; changes are promoted via pull request rather than direct pushes where your team requires it.
- Run `npm test` before pushing when you have changed application code.
